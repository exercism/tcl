#!/usr/bin/env bash
dir=$(cd "$(dirname "$0")" >/dev/null && pwd)
cd "$dir/.."

shopt -s nullglob
status=0

for ex in exercises/concept/* exercises/practice/*; do
    "$dir"/test_one_exercise "$ex" || status=1
done

exit "$status"
